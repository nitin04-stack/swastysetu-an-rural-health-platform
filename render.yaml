services:
  - type: web
    name: gramin-care-access
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && gunicorn --workers 1 --threads 1 --max-requests 20 --max-requests-jitter 5 --timeout 120 --bind 0.0.0.0:$PORT --access-logfile - --error-logfile - --capture-output --log-level info app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        sync: false
      - key: FLASK_DEBUG
        value: "0"
      - key: DEV_HTTPS
        value: "0"
      - key: PYTHON_VERSION
        value: "3.11.9"
